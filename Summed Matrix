class Solution {
  public:
    long long sumMatrix(long long n, long long q) {
        // code here
        if(q>2*n||q<2){ // q is greater then the max value or less then
            return 0;
        }
        return n-abs(n+1-q);
          
        
    }
};
